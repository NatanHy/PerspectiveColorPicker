# PerspectiveColorPicker
Find color matches for perspective builds.

# Cloning and Building

clone with `--recurse-submodules` to get external libraries cloned, or use `git submodule update --init --recursive`.

Build raylib with `cd external/raylib/src && make PLATFORM=PLATFORM_DESKTOP`.

Build project with `cmake --build build`.